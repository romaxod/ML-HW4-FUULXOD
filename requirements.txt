# Colab already ships torch/torchvision/numpy/pandas/scikit-learn/matplotlib.
# The notebooks `pip install wandb` themselves. This file is for running the
# notebooks locally (e.g. jupyter) if you don't use Colab.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
wandb>=0.16.0
kaggle>=1.6
